import { Socket } from "socket.io-client";

/*
  Listeners which handles commands regarding UltraGrids software including its initialization, heartbeat and stream 
  management. These requests will be made from an Orchestrator that this XR-IT Node is currently connected to.
*/
export default (socket: Socket) => {

};
